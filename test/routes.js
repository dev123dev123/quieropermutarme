var assert = require('assert');

describe('User', function(){
	describe('#indexOf()', function(){
		it('should return error trying to save duplicate useremail', function(){

			
			assert.equal(-1, [1,2,3].indexOf(1));
		});
	});
});